const sumIndexTarget = (arr: Number[], target: Number): Number[] | null => {
    const result = null;
    if (arr.length > 0 && target) {
        // is not null
    }
    return result
}

export default sumIndexTarget;